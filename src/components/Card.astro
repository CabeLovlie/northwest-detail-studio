---
interface Props {
  class?: string;
  padding?: boolean;
}

const { class: className = '', padding = true } = Astro.props;
---

<div class:list={['glass-card', padding && 'p-6 sm:p-8', className]}>
  <slot />
</div>

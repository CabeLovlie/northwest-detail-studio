---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<p class:list={['text-sm font-semibold uppercase tracking-widest text-accent', className]}>
  <slot />
</p>
